export function DemoForm({title,fields}:{title:string;fields:Array<[string,string,boolean?]>}){return <section className="card"><h2 className="cardTitle">{title}</h2><div className="formGrid" style={{marginTop:14}}>{fields.map(([label,value,wide])=><div className={`field ${wide?"wide":""}`} key={label}><label>{label}</label><input className="input" defaultValue={value}/></div>)}</div><button className="button primary" style={{marginTop:14}}>Save changes</button></section>}
